{
  "name": "tebno-netlify-store",
  "private": true,
  "version": "1.0.0",
  "description": "TeBno tienda empresarial lista para Netlify con tiempo real simple",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "dev": "npx netlify dev",
    "deploy": "npx netlify deploy --prod"
  },
  "dependencies": {
    "@netlify/blobs": "^8.1.0"
  }
}
